export enum PaymentMethodEnum {
    PAYPAL = 'PAYPAL',
    STRIPE = 'STRIPE'
}

export enum PaymentMethodStatus {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE'
}

export enum PaymentProvider {
    PAYPAL = "paypal",
    STRIPE = "stripe",
}