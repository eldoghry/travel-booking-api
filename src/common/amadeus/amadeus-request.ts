export enum AmadeusEndpoints {
  // Login routes
  AUTHENTICATE = '/v1/security/oauth2/token',

  // Flight routes
  FLIGHT_SEARCH = '/v2/shopping/flight-offers',
  FLIGHT_DETAILS = '/v2/shopping/flight-offers/:id',
  FLIGHT_BOOKING = '/v2/booking/flight-orders',

  // Hotel routes
}
